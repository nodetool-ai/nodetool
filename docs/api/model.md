# nodetool.api.model

### function_model

**Args:**
- **user (User) (default: Depends(current_user))**

**Returns:** list

### index

**Args:**
- **folder (str)**
- **user (User) (default: Depends(current_user))**

**Returns:** list

### llama_model

**Args:**
- **user (User) (default: Depends(current_user))**

**Returns:** list

