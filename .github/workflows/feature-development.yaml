name: Feature Development

on:
  schedule:
    - cron: "0 */6 * * *"
  workflow_dispatch:

jobs:
  feature-development:
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: write
      pull-requests: write
      issues: write
      actions: read
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"
          cache-dependency-path: web/package-lock.json

      - name: Install dependencies
        run: |
          cd web && npm ci
          cd ../electron && npm ci

      - name: Pre-flight quality check
        id: pre-check
        continue-on-error: true
        run: |
          echo "## Pre-Flight Quality Check" >> $GITHUB_STEP_SUMMARY
          TYPECHECK_EXIT=0; LINT_EXIT=0; TEST_EXIT=0

          if make typecheck 2>&1 | tee typecheck-pre.log; then
            echo "✅ TypeScript: OK" >> $GITHUB_STEP_SUMMARY
          else
            TYPECHECK_EXIT=1
            echo "⚠️ TypeScript errors found" >> $GITHUB_STEP_SUMMARY
          fi

          if make lint 2>&1 | tee lint-pre.log; then
            echo "✅ ESLint: OK" >> $GITHUB_STEP_SUMMARY
          else
            LINT_EXIT=1
            echo "⚠️ Lint errors found" >> $GITHUB_STEP_SUMMARY
          fi

          if make test 2>&1 | tee test-pre.log; then
            echo "✅ Tests: OK" >> $GITHUB_STEP_SUMMARY
          else
            TEST_EXIT=1
            echo "⚠️ Test failures found" >> $GITHUB_STEP_SUMMARY
          fi

          echo "TYPECHECK_PRE_EXIT=$TYPECHECK_EXIT" >> $GITHUB_ENV
          echo "LINT_PRE_EXIT=$LINT_EXIT" >> $GITHUB_ENV
          echo "TEST_PRE_EXIT=$TEST_EXIT" >> $GITHUB_ENV

      - name: Run Claude Code
        uses: anthropics/claude-code-action@v1
        env:
          ANTHROPIC_BASE_URL: https://api.z.ai/api/anthropic
        with:
          anthropic_api_key: ${{ secrets.ZAI_API_KEY }}
          additional_permissions: |
            actions: read
          claude_args: |
            --allowedTools "Bash,WebFetch,CreatePullRequest,Edit,Read,Replace"
          prompt: |
            # Feature Development

            Develop ONE polished feature for NodeTool (React/TypeScript visual AI workflow builder).

            ## ⚠️ IMPORTANT: You MUST create a Pull Request
            Your job is to:
            1. Choose or invent a useful feature to implement
            2. Develop it completely with tests
            3. **Create a Pull Request with your feature**
            
            Do NOT just plan or prototype. You must implement a complete, tested feature and submit a PR.

            ## Context
            - Stack: React 18.2, TypeScript 5.7, Zustand, ReactFlow, MUI v7
            - Packages: `/web` (React app), `/electron` (desktop), `/mobile` (React Native)
            - Backend is a separate Python repo — frontend-only changes here
            - Memory: `.memory/` contains project context, features list, and known issues

            ## Before Starting — MANDATORY DEDUPLICATION
            1. Run `git branch -a` — avoid duplicating existing feature branches
            2. **Run `gh pr list --state open --limit 50`** — if a similar PR is already open, STOP and pick a different feature
            3. **Read `.memory/features.md`** — the top section lists features that already have open PRs and must NOT be duplicated
            4. Read `.memory/project-context.md` — understand architecture and recent changes
            5. Read `.memory/insights/git-ci/pr-acceptance-patterns.md` — understand what gets merged vs rejected
            6. Read `/AGENTS.md` and `/.github/copilot-instructions.md` for coding patterns

            ## ❌ DO NOT Build These (already have open PRs or have been repeatedly rejected)
            - Keyboard shortcuts panel/dialog/help/toast/cheat-sheet
            - Workflow notes, comments, or documentation panel
            - Node templates or snippet system
            - Viewport or workflow bookmarks
            - Node pinning, tab pinning
            - Canvas pan controls
            - Smart alignment guides
            - "Audit report" markdown documents
            - Any feature already listed in the "Do NOT Create PRs For These" section of `.memory/features.md`

            ## ✅ High-Acceptance Feature Ideas
            - Concrete bug fix with tests (check open issues for reported bugs)
            - Improvement to an existing feature (make something work better, not add new panels)
            - Test coverage for untested components or hooks
            - Performance improvement with measured evidence (use React DevTools to confirm the bottleneck first)
            - Accessibility improvement to a specific existing component

            ## Quality Standards
            - TypeScript strict mode, explicit types, no `any`
            - Functional components with typed props
            - Selective Zustand subscriptions (not full store)
            - MUI theme values (not hardcoded colors/spacing)
            - Tests for new functionality (Jest + React Testing Library)

            ## Mandatory Quality Checks
            Before submitting your PR, you MUST run and pass all quality checks:
            ```bash
            make lint       # All lint checks must pass
            make typecheck  # All type checks must pass
            make test       # All tests must pass
            ```
            All three commands must exit with code 0. Fix any errors before proceeding.

            ## Code Review for Regressions
            After making changes, review your code for potential regressions:
            - Run the affected features manually to ensure they work
            - Check that existing tests still pass
            - Look for unintended side effects in related code
            - Verify no new TypeScript errors or lint warnings were introduced
            - Test edge cases and error handling

            ## Final Step: CREATE THE PULL REQUEST
            Once you've completed your feature and all quality checks pass, you MUST:
            1. Commit all your changes (implementation + tests)
            2. Push your branch
            3. **Open a Pull Request** with:
               - Clear title and description explaining what the feature does and why it's useful
               - Quality check results (make lint, make typecheck, make test all passing)
               - Any potential regressions you identified and how you addressed them

      - name: Post-change verification
        if: always()
        run: |
          echo "## Post-Change Verification" >> $GITHUB_STEP_SUMMARY
          TYPECHECK_EXIT=0; LINT_EXIT=0; TEST_EXIT=0

          if make typecheck 2>&1 | tee typecheck-post.log; then
            echo "✅ TypeScript: OK" >> $GITHUB_STEP_SUMMARY
          else
            TYPECHECK_EXIT=1
            echo "❌ TypeScript errors" >> $GITHUB_STEP_SUMMARY
          fi

          if make lint 2>&1 | tee lint-post.log; then
            echo "✅ ESLint: OK" >> $GITHUB_STEP_SUMMARY
          else
            LINT_EXIT=1
            echo "❌ Lint errors" >> $GITHUB_STEP_SUMMARY
          fi

          if make test 2>&1 | tee test-post.log; then
            echo "✅ Tests: OK" >> $GITHUB_STEP_SUMMARY
          else
            TEST_EXIT=1
            echo "❌ Test failures" >> $GITHUB_STEP_SUMMARY
          fi

          # Fail if quality degraded
          if [ "$TYPECHECK_PRE_EXIT" -eq 0 ] && [ $TYPECHECK_EXIT -ne 0 ]; then
            echo "🔴 NEW TypeScript errors introduced!" >> $GITHUB_STEP_SUMMARY
            exit 1
          fi
          if [ "$LINT_PRE_EXIT" -eq 0 ] && [ $LINT_EXIT -ne 0 ]; then
            echo "🔴 NEW lint errors introduced!" >> $GITHUB_STEP_SUMMARY
            exit 1
          fi
          if [ "$TEST_PRE_EXIT" -eq 0 ] && [ $TEST_EXIT -ne 0 ]; then
            echo "🔴 NEW test failures introduced!" >> $GITHUB_STEP_SUMMARY
            exit 1
          fi
