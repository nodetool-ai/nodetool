name: Performance Monitor

on:
  schedule:
    - cron: "20 */6 * * *"
  workflow_dispatch:

jobs:
  performance-monitor:
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: write
      pull-requests: write
      issues: write
      actions: read
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"
          cache-dependency-path: web/package-lock.json

      - name: Install web dependencies
        run: cd web && npm ci

      - name: Pre-flight quality check
        id: pre-check
        continue-on-error: true
        run: |
          echo "## Pre-Flight Quality Check" >> $GITHUB_STEP_SUMMARY
          TYPECHECK_EXIT=0; LINT_EXIT=0; TEST_EXIT=0

          if make typecheck 2>&1 | tee typecheck-pre.log; then
            echo "✅ TypeScript: OK" >> $GITHUB_STEP_SUMMARY
          else
            TYPECHECK_EXIT=1
            echo "⚠️ TypeScript errors found" >> $GITHUB_STEP_SUMMARY
          fi

          if make lint 2>&1 | tee lint-pre.log; then
            echo "✅ ESLint: OK" >> $GITHUB_STEP_SUMMARY
          else
            LINT_EXIT=1
            echo "⚠️ Lint errors found" >> $GITHUB_STEP_SUMMARY
          fi

          if make test 2>&1 | tee test-pre.log; then
            echo "✅ Tests: OK" >> $GITHUB_STEP_SUMMARY
          else
            TEST_EXIT=1
            echo "⚠️ Test failures found" >> $GITHUB_STEP_SUMMARY
          fi

          echo "TYPECHECK_PRE_EXIT=$TYPECHECK_EXIT" >> $GITHUB_ENV
          echo "LINT_PRE_EXIT=$LINT_EXIT" >> $GITHUB_ENV
          echo "TEST_PRE_EXIT=$TEST_EXIT" >> $GITHUB_ENV

      - name: Analyze bundle size
        run: |
          cd web
          npm run build
          npx vite-bundle-visualizer --open=false --json=bundle-stats.json || true

      - name: Run Claude Code
        uses: anthropics/claude-code-action@v1
        env:
          ANTHROPIC_BASE_URL: https://api.z.ai/api/anthropic
        with:
          anthropic_api_key: ${{ secrets.ZAI_API_KEY }}
          additional_permissions: |
            actions: read
          claude_args: |
            --allowedTools "Bash,WebFetch,CreatePullRequest,Edit,Read,Replace"
          prompt: |
            # Performance Optimization

            Identify and fix performance bottlenecks in NodeTool (React/TypeScript visual AI workflow builder).

            ## ⚠️ IMPORTANT: You MUST create a Pull Request
            Your job is to:
            1. Find performance bottlenecks
            2. Fix them
            3. **Create a Pull Request with your optimizations**
            
            Do NOT just analyze or report findings. You must make optimizations and submit a PR.

            ## Context
            - Stack: React 18.2, TypeScript 5.7, Zustand, ReactFlow, MUI v7
            - Packages: `/web`, `/electron`, `/mobile`
            - Performance-critical: node editor (100+ nodes), asset management (1000+ files)
            - Memory: `.memory/` contains project context and known issues

            ## Before Starting — MANDATORY DEDUPLICATION
            1. Run `git branch -a` — avoid duplicating existing performance branches
            2. **Run `gh pr list --state open --limit 50`** — if a similar performance PR is already open, STOP and pick a different optimization target
            3. Read `.memory/project-context.md` and `.memory/features.md`
            4. Read `.memory/insights/git-ci/pr-acceptance-patterns.md` — understand what performance PRs get merged vs rejected

            ## ❌ DO NOT Do These (they get rejected)
            - Bulk React.memo additions across many components without profiling evidence
            - "Add React.memo to N components" as the main goal
            - Performance "audit report" documents
            - Optimizations for components that don't actually re-render unnecessarily

            ## ✅ High-Acceptance Performance Fixes
            These specific patterns are known to cause real performance issues:
            - **Zustand object selector anti-pattern**: `const { x, y } = useStore()` → should be separate selectors
            - **Inline function in memoized component prop**: `onClick={() => fn(id)}` without useCallback, passed to React.memo child
            - **Missing cleanup in useEffect**: event listeners, timers, subscriptions leaking
            - **Expensive computation in render**: heavy filter/map/sort without useMemo

            ## Audit Areas

            **React re-renders**: Find components with `useStore()` without selectors (subscribes to whole store). Find `React.memo`-wrapped children receiving new inline function references each render.

            **List rendering**: Large lists without virtualization (use `@tanstack/react-virtual`).

            **Bundle size**: Check `web/bundle-stats.json` for large/duplicate/unused dependencies.

            **Memory leaks**: `useEffect` without cleanup (event listeners, timers, subscriptions).

            **Heavy computations**: Filter/map/sort/JSON.parse in render paths — memoize them.

            ## Fix Guidelines
            - **Measure before optimizing** — confirm the bottleneck actually exists
            - One focused PR per optimization area (NOT bulk changes to 10+ files)
            - Maximum 5-8 files per PR

            ## Mandatory Quality Checks
            Before submitting your PR, you MUST run and pass all quality checks:
            ```bash
            make lint       # All lint checks must pass
            make typecheck  # All type checks must pass
            make test       # All tests must pass
            ```
            All three commands must exit with code 0. Fix any errors before proceeding.

            ## Code Review for Regressions
            After making changes, review your code for potential regressions:
            - Run the affected features manually to ensure they work
            - Check that existing tests still pass
            - Look for unintended side effects in related code
            - Verify no new TypeScript errors or lint warnings were introduced
            - Test edge cases and error handling
            - Verify performance improvements don't break functionality

            ## Final Step: CREATE THE PULL REQUEST
            Once you've made your performance improvements, you MUST:
            1. Commit all your changes
            2. Push your branch
            3. **Open a Pull Request** with:
               - Clear description of what was slow and how you fixed it
               - Quality check results (make lint, make typecheck, make test all passing)
               - Any potential regressions you identified and how you addressed them

      - name: Post-change verification
        if: always()
        run: |
          echo "## Post-Change Verification" >> $GITHUB_STEP_SUMMARY
          TYPECHECK_EXIT=0; LINT_EXIT=0; TEST_EXIT=0

          if make typecheck 2>&1 | tee typecheck-post.log; then
            echo "✅ TypeScript: OK" >> $GITHUB_STEP_SUMMARY
          else
            TYPECHECK_EXIT=1
            echo "❌ TypeScript errors" >> $GITHUB_STEP_SUMMARY
          fi

          if make lint 2>&1 | tee lint-post.log; then
            echo "✅ ESLint: OK" >> $GITHUB_STEP_SUMMARY
          else
            LINT_EXIT=1
            echo "❌ Lint errors" >> $GITHUB_STEP_SUMMARY
          fi

          if make test 2>&1 | tee test-post.log; then
            echo "✅ Tests: OK" >> $GITHUB_STEP_SUMMARY
          else
            TEST_EXIT=1
            echo "❌ Test failures" >> $GITHUB_STEP_SUMMARY
          fi

          # Fail if quality degraded
          if [ "$TYPECHECK_PRE_EXIT" -eq 0 ] && [ $TYPECHECK_EXIT -ne 0 ]; then
            echo "🔴 NEW TypeScript errors introduced!" >> $GITHUB_STEP_SUMMARY
            exit 1
          fi
          if [ "$LINT_PRE_EXIT" -eq 0 ] && [ $LINT_EXIT -ne 0 ]; then
            echo "🔴 NEW lint errors introduced!" >> $GITHUB_STEP_SUMMARY
            exit 1
          fi
          if [ "$TEST_PRE_EXIT" -eq 0 ] && [ $TEST_EXIT -ne 0 ]; then
            echo "🔴 NEW test failures introduced!" >> $GITHUB_STEP_SUMMARY
            exit 1
          fi
