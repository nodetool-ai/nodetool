{
  "name": "@nodetool/protocol",
  "version": "0.1.0",
  "description": "Shared message types and protocol definitions for the NodeTool workflow runtime",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vitest": "^1.6.1"
  }
}
