/**
 * `@nodetool-ai/blender-nodes` — Blender headless integration (Stage 1a).
 *
 * This run ships the substrate only: binary discovery, the versioned job
 * contract, the runner interface with its local implementation, and the thin
 * `runBlenderJob` entry point. No nodes yet — `BLENDER_NODES` stays empty
 * until the node run fills it in.
 */

export const BLENDER_NODES: readonly never[] = [];
