# @nodetool-ai/kie-codegen

Generates TypeScript node classes for `kie-nodes` from configs in `src/configs/`. Unlike fal/replicate, there is no schema fetcher.
https://docs.kie.ai/llms.txt - machine-readable index with all model endpoints.

```bash
npm run generate:kie
```
