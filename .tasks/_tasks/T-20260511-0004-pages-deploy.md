---
id: T-20260511-0004
title: "Deploy task site to GitHub Pages alongside docs"
state: done
plan: P-2026-05-11-task-system
assignee: claude
dependencies: [T-20260511-0002]
created: 2026-05-11T13:30:00Z
updated: 2026-05-11T14:30:00Z
estimate: 1h
tags: [ci, deployment]
---
# Description

Extend the existing `.github/workflows/jekyll.yml` to also build the
`.tasks/` site and merge it into the Pages artifact at `/tasks/`.
Also add a separate `tasks-validate.yml` workflow that runs
`npm run task validate` on every PR touching `.tasks/` for fast
schema feedback without waiting for a full build.

# Acceptance criteria

- [x] `jekyll.yml` builds `.tasks/` and copies output into `_site/tasks/`
- [x] `tasks-validate.yml` runs the linter on PRs touching `.tasks/**` or `scripts/tasks.mjs`
- [x] Local `cd .tasks && bundle exec jekyll build` produces `_site/` cleanly

# Notes

## 2026-05-11 — claude

The repo already has a Pages deploy from `docs/`. Only one Pages
deploy can be active per repo, so I'll merge into the existing
artifact rather than create a competing workflow.

## 2026-05-11 — claude

Verified locally: `bundle exec jekyll build --baseurl /tasks`
produces `_site/index.html` (Kanban dashboard), `_site/plans/<id>/`,
and `_site/tasks/<id>/` for every plan and task. Fixed one Liquid
warning (sort filter required intermediate `assign`).
→ done.
