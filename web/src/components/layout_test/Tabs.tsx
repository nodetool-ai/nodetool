/** @jsxImportSource @emotion/react */
import { css } from "@emotion/react";
import { Tabs } from "@mui/material";

const styles = { root: css`` };

const TabsDemo = () => <Tabs css={styles.root}>Tabs</Tabs>;

export default TabsDemo;
