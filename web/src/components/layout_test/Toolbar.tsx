/** @jsxImportSource @emotion/react */
import { css } from "@emotion/react";
import { Toolbar } from "@mui/material";

const styles = { root: css`` };

const ToolbarDemo = () => <Toolbar css={styles.root}>Toolbar</Toolbar>;

export default ToolbarDemo;
