/** @jsxImportSource @emotion/react */
import { css } from "@emotion/react";
import { Grid } from "@mui/material";

const styles = { root: css`` };

const GridDemo = () => <Grid css={styles.root}>Grid</Grid>;

export default GridDemo;
