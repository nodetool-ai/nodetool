/** @jsxImportSource @emotion/react */
import { css } from "@emotion/react";
import { Alert } from "@mui/material";

const styles = { root: css`` };

const AlertDemo = () => <Alert css={styles.root}>Alert</Alert>;

export default AlertDemo;
