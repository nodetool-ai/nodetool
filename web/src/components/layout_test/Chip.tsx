/** @jsxImportSource @emotion/react */
import { css } from "@emotion/react";
import { Chip } from "@mui/material";

const styles = { root: css`` };

const ChipDemo = () => <Chip label="Chip" css={styles.root} />;

export default ChipDemo;
