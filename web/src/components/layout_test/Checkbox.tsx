/** @jsxImportSource @emotion/react */
import { css } from '@emotion/react';
import { Checkbox } from '@mui/material';

const styles = { root: css`` };

const CheckboxDemo = () => <Checkbox css={styles.root} />;

export default CheckboxDemo;
