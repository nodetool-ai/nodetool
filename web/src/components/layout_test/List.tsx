/** @jsxImportSource @emotion/react */
import { css } from "@emotion/react";
import { List } from "@mui/material";

const styles = { root: css`` };

const ListDemo = () => <List css={styles.root}>List</List>;

export default ListDemo;
