/** @jsxImportSource @emotion/react */
import { css } from "@emotion/react";
import { Fade } from "@mui/material";

const styles = { root: css`` };

const FadeDemo = () => <Fade css={styles.root}>Fade</Fade>;

export default FadeDemo;
