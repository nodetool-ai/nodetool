/** @jsxImportSource @emotion/react */
import { css } from "@emotion/react";
import { Slider } from "@mui/material";

const styles = { root: css`` };

const SliderDemo = () => <Slider css={styles.root}>Slider</Slider>;

export default SliderDemo;
