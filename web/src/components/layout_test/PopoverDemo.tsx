/** @jsxImportSource @emotion/react */
import { css } from "@emotion/react";
import { Popover } from "@mui/material";

const styles = { root: css`` };

const PopoverDemo = () => <Popover css={styles.root}>Popover</Popover>;

export default PopoverDemo;
