/** @jsxImportSource @emotion/react */
import { css } from "@emotion/react";
import { Tooltip } from "@mui/material";

const styles = { root: css`` };

const TooltipDemo = () => <Tooltip css={styles.root}>Tooltip</Tooltip>;

export default TooltipDemo;
