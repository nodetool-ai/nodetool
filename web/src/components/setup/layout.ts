/**
 * Widths shared by the guided-flow steps.
 *
 * A short value does not need the whole column: an aspect ratio or a model name
 * reads as a field at this width and as a banner at full bleed.
 */
export const SETUP_FIELD_WIDTH = 320;
