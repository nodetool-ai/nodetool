/**
 * GPU helper utilities barrel.
 *
 * Re-exports small WebGPU helpers (bind-group builders, uniform packing,
 * pipeline caches, etc.) as they are migrated in later tasks.
 *
 * Stub — will be populated by T-20260512-0004.
 */

export {};
