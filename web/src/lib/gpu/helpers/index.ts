/**
 * Shared GPU helper barrel.
 *
 * Phase 1: stub. Helper implementations (fullscreen pass builders, uniform
 * buffer helpers, render-texture helpers, etc.) will be moved here in later
 * tasks and re-exported below.
 */

export {};
