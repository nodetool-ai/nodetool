// APP
export const APP_NAME = "nodetool";
export const VERSION = "0.1.6-alpha";

// TOOLTIPS
export const TOOLTIP_DELAY = 500;

// DEVLOG
export const DEVLOG_NOTIFICATION_VERBOSITY: ("error" | "info" | "warn")[] = [
  "info",
  "error",
  "warn"
];

// NOTIFICATIONS
export const NOTIFICATIONS_LIST_MAX_ITEMS = 100;

// CHAT
export const CHAT_HISTORY_AMOUNT = 10;

// EDITOR
export const DUPLICATE_SPACING_X = 300;
export const MIN_ZOOM = 0.4;
export const MAX_ZOOM = 4.0;
