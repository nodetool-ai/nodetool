{
  "//": "Standalone typecheck for the showcase seeder (Node tooling, run with tsx). Kept out of the Next app tsconfig because it imports backend workspace packages that must not enter the web build.",
  "compilerOptions": {
    "lib": ["esnext"],
    "module": "esnext",
    "moduleResolution": "bundler",
    "target": "es2022",
    "strict": true,
    "skipLibCheck": true,
    "esModuleInterop": true,
    "resolveJsonModule": true,
    "noEmit": true,
    "types": ["node"]
  },
  "include": ["**/*.ts"],
  "exclude": ["out"]
}
