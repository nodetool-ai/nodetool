Execute immediately. Do not search, inspect schemas, read docs, or use run_search. No browser/UI/CUA. Use only `get_storyboard`, `edit_image`, `edit_storyboard`, and `animate_image`.

Reconcile storyboard `af795795297f43bf9e66d0318f422c83`. Immutable input `asset://382922e1ca1543468d59f0af5575350e.png`. The provider string MUST be exactly `fal_ai` for every media call. Never use `fal`. FAL image model `fal-ai/bytedance/seedream/v4.5/edit`. Generate exactly two targeted retries:

- AD-S4 prompt: `Photographic quiet morning kitchen, warm left window light, pale stone. Exactly one Olive Travel Cup, fully isolated in frame. No second cup, no partial cup, no cropped cup at any edge, no duplicate lid, no extra object. Preserve the reference muted-olive tapered body, single charcoal flat lid and one rectangular opening. Front three-quarter hero view, cup below center, uncluttered space above.`
- AD-S1C prompt: `Photographic quiet morning kitchen, warm left window light, pale stone. Exactly one Olive Travel Cup and no other object. No dangling strand, hook, handle, cable, seam, accessory or artifact. Preserve the reference muted-olive tapered body, single charcoal flat lid and one rectangular opening. Slightly elevated close view showing lid and upper body, space below.`

Use `edit_image` with input_file and reference_files both set to the immutable asset and provider exactly `fal_ai`. After each succeeds, call `edit_storyboard` with one `add_keyframe_version` op targeting the exact slug and its asset_id. Then use `animate_image` with provider exactly `fal_ai` and model `minimax/h3/image-to-video` for AD-S4 motion `Very small push for the first two seconds, then a stable hold. No object transformation or background movement.` and AD-S1C motion `Slow 10-degree camera arc. The lid opening remains fixed on the product. No full orbit or newly invented backside detail.` Print only new still IDs, clip IDs, generation IDs and failures. Do not package or generate anything else.
